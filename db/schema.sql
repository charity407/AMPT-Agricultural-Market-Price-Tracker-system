-- Schema for AMPT database
-- You can paste the dump from `database_setup.sql` here or keep
the original file name
and use this as a reference.-- Example:
CREATE TABLE public.markets (
    market_id integer NOT NULL,
    market_name character varying(255) NOT NULL,
    --     ...
    -- );
